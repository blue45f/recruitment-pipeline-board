# DECISIONS

## 1. 함께 변경되는 코드를 같은 도메인에 둔다

채용 보드의 모델, API, 조회, 단계 이동, Undo는 `src/domains/recruitment` 안에 둔다. 역할 이름만으로 전역 폴더를 늘리면 한 기능을 수정할 때 여러 위치를 오가게 되므로 사용하지 않는다. `app`은 조립, `routes`는 URL 진입점, `components`와 `lib`는 둘 이상의 영역에서 재사용하는 코드만 맡는다.

## 2. 상태의 출처를 나눈다

서버에서 온 후보자 목록과 mutation은 TanStack Query가 관리한다. 이름과 직무 조건은 URL에 두고, 현재 연 후보자 ID는 새로고침과 무관한 Zustand 상태로 관리한다. 표시할 데이터 수는 Zod로 검증한 뒤 Zustand persist에 저장한다. 폼 입력과 검증은 React Hook Form과 Zod가 맡는다. 같은 값을 여러 저장소에 복제하는 방식은 동기화 오류가 생기기 쉬워 사용하지 않는다.

## 3. 명시적 단계 이동을 기준 동작으로 삼는다

키보드와 화면 읽기 도구에서도 같은 기능을 쓸 수 있도록 명시적인 단계 이동 액션을 기준으로 구현한다. 포인터 드래그 앤 드롭은 이를 대체하지 않는 보조 입력으로 추가한다. 두 입력 방식은 같은 mutation 경로를 사용해 동작 차이가 생기지 않게 한다.

## 4. 낙관적 갱신은 후보자별로 직렬화한다

단계 변경 API는 `expectedRevision`을 최신 저장값과 비교한 뒤 동기적으로 저장한다. 클라이언트에서도 후보자 단위로 요청 순서를 관리하고 서버가 마지막으로 확정한 상태를 rollback 기준으로 삼는다. 전체 목록을 매번 다시 받아 덮어쓰는 방식은 빠른 연속 이동에서 오래된 응답이 최신 상태를 되돌릴 수 있어 사용하지 않는다.

## 5. 병렬 조회를 만들기 전에 요청 수를 줄인다

보드에 필요한 후보자 목록은 한 번에 조회하고 화면에서 단계별로 나눈다. 독립된 쿼리가 생길 때만 Suspense 경계 안에서 병렬로 시작한다. 필요하지 않은 `useSuspenseQueries`를 먼저 도입하는 대신 실제 요청 구조를 기준으로 waterfall 여부를 측정한다.

## 6. 조회 오류는 보드 안에서 복구한다

후보자 목록의 Suspense와 Query 오류 경계는 보드 영역에 둔다. 목록 조회가 실패해도 애플리케이션 제목과 기본 안내는 유지하고, 서버 원문 대신 API 경계에서 정규화한 안전한 문구만 보여 준다. 다시 시도할 때는 Query의 오류 상태와 Error Boundary를 함께 초기화해 같은 화면에서 복구한다. 키보드로 재시도한 경우에는 사라지는 버튼 대신 계속 남아 있는 보드 영역으로 포커스를 옮기고, 복구된 인원은 status로 알린다.

전역 QueryClient는 도메인 오류의 이름이나 필드 형태를 추측하지 않고 기본 재시도를 하지 않는다. 후보자 query option에서 실제 `ApiError` 인스턴스이면서 재시도 가능한 조회만 한 번 허용한다. 오류 의미를 소유한 도메인과 실행 정책을 함께 두어 형태가 비슷한 일반 오류가 요청을 반복하지 않게 한다.

## 7. 검색 조건은 URL에 두고 조회한 목록 안에서 적용한다

이름과 직무 조건은 URL query parameter에 저장해 새로고침과 직접 진입에서도 복원한다. 각 값을 따로 검증해 잘못된 직무 때문에 유효한 검색어까지 버리지 않으며 기본값은 URL에서 생략한다. 필터링은 선택한 표시 수로 조회한 후보자 목록 안에서만 수행하므로 이름이나 직무가 바뀌어도 목록 API를 다시 호출하지 않고 표시 수가 바뀔 때만 새 query를 시작한다. 입력 반응성은 `useDeferredValue`로 보호하고 지연 중에는 직전 결과를 유지한 채 보드 영역의 busy 상태만 알린다.

## 8. 상세 선택과 상세 데이터의 소유권을 분리한다

현재 연 후보자 ID만 Zustand에 두고 상세 응답, 로딩, 오류와 캐시는 TanStack Query가 관리한다. 카드가 포인터나 키보드 포커스를 120ms 이상 유지했을 때만 상세 조회를 미리 시작한다. 모달은 같은 query option을 사용해 진행 중인 요청이나 신선한 캐시를 이어받는다. 빠르게 여러 카드를 지나갈 때마다 요청하거나 조회한 후보자 전체를 미리 요청하는 방식은 사용하지 않는다.

상세의 Suspense와 Error Boundary는 목록 경계와 분리한다. 상세 요청이 실패해도 보드는 유지하고 재시도 가능한 오류에만 동작을 제공한다. 모달을 닫으면 원래 카드로 포커스를 돌려주며 필터나 표시 수 변경으로 카드가 사라졌다면 계속 남아 있는 보드 영역을 대신 사용한다.

## 9. 시각 기준선은 같은 실행 환경에서만 판단한다

시각 회귀 기준선은 고정된 Linux·Chrome·Noto CJK 조합에서 만들고 같은 환경에서 비교한다. macOS에서 만든 이미지는 운영체제와 글꼴 렌더링이 달라 기준선으로 사용하지 않는다. 기준선은 자동으로 갱신하지 않고 실제 화면을 확인한 네 장만 추적한다. 비교 결과가 다르면 실제 이미지와 차이 이미지를 CI 산출물로 확인한다. 픽셀 색상 임계값과 전체 불일치 허용 비율은 0으로 유지하고 안티앨리어싱 차이도 비교에서 제외하지 않는다.

## 10. 단계별 목록만 세로로 가상화한다

보드 전체의 가로 배치는 유지하고 각 단계 안의 긴 세로 목록만 가상화한다. 카드 높이를 고정값으로 가정하지 않고 실제 요소를 측정해 기본 글자 크기나 텍스트 간격이 달라져도 카드가 겹치지 않게 한다. DOM에는 일부 카드만 있어도 `list`와 `listitem` 의미, 전체 개수와 논리적 위치를 전달한다.

가상 목록마다 하나의 탭 정지점만 두고 방향키와 Home, End로 이동한다. 포인터 스크롤 뒤 목록을 벗어날 때는 현재 보이는 카드로 다음 탭 위치를 맞춘다. 후보자 상세를 열어 둔 동안 해당 카드는 렌더 범위에 남겨 닫은 뒤 포커스를 되돌린다. 이름, 직무, 표시 수가 바뀔 때만 목록 위치를 처음으로 되돌리되 다른 화면 상태 변화에는 기존 스크롤을 유지한다.

## 아직 구현하지 않은 항목

후보자 계약, Mock API와 API 클라이언트, 공용 디자인 시스템, 다섯 단계 보드, 0명·200명·1,000명 목록, 전용 조회 상태, 검색·필터, 후보자 상세, 단계별 가상 목록과 시각 회귀 자동 비교는 구현했다. 단계 이동, 낙관적 갱신과 rollback, 경쟁 상태 처리, Undo와 포인터 드래그 앤 드롭은 다음 기능에서 구현한다.
